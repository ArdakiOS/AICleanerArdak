
import SwiftUI

struct CompressionView: View {
    var body: some View {
        ZStack{
            Color(hex: "#0E0F10").ignoresSafeArea()
            VStack{
                Text("Page CompressionView")
            }
        }
    }
}

#Preview {
    CompressionView()
}
